apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: englishservice-monitor
  # namespace: default 
  labels:
    release: prometheus
    # team: rss-account-service
spec:
  jobLabel: english-app
  selector:
    matchLabels:
      app: "english-greeter"
  namespaceSelector:
    matchNames: 
    - default
  endpoints:
  - port: http
    path: /actuator/prometheus
    interval: 10s